name: Xcode - Build and Analyze

on:
  push:
    branches: [ "develop" ]
  pull_request:
    branches: [ "develop" ]

jobs:
  build:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v2
      - name: Start xcode build 🛠
        run: |
          xcodebuild clean test -project DIMI_FRIEND/DIMI_FRIEND.xcodeproj -scheme DIMI_FRIEND -destination 'platform=iOS Simulator,name=iPhone 11 Pro,OS=14.4'